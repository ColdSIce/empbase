import { InSelectedDivsPipe } from './in-selected-divs.pipe';

describe('InSelectedDivsPipe', () => {
  it('create an instance', () => {
    const pipe = new InSelectedDivsPipe();
    expect(pipe).toBeTruthy();
  });
});
