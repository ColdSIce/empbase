import { SkillByGroupIdPipe } from './skill-by-group-id.pipe';

describe('SkillByGroupIdPipe', () => {
  it('create an instance', () => {
    const pipe = new SkillByGroupIdPipe();
    expect(pipe).toBeTruthy();
  });
});
