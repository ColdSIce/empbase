import { SkillByGroupPipe } from './skill-by-group.pipe';

describe('SkillByGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new SkillByGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
