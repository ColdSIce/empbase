import { ListByPropPipe } from './list-by-prop.pipe';

describe('ListByPropPipe', () => {
  it('create an instance', () => {
    const pipe = new ListByPropPipe();
    expect(pipe).toBeTruthy();
  });
});
