import { RoomByOfficePipe } from './room-by-office.pipe';

describe('RoomByOfficePipe', () => {
  it('create an instance', () => {
    const pipe = new RoomByOfficePipe();
    expect(pipe).toBeTruthy();
  });
});
